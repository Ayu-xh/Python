# This file is just going to be for keeping notes.

# Numbers:

# Classic division (/) always returns a float value, whereas floor division (//) always returns the integer part of the answer.

# Also, the modulo operator (%) returns the remainder.

# Strings:

# Strings are immutable by default (and cannot be changed).

# We can assign a new string to a variable, but cannot change specific indices in a given string.

# Lists:

# Lists are mutable by default.

# To add a new item to the end of the list, we can use the append method.
